* Запуск в dev режиме:
* Backend:
  - cd server
  - npm ci
  - npm run dev
* Frontend
  - cd client
  - npm ci
  - npm run dev
* DB: postgresql конфигурация в ./server/.env.development


* Prod:
  - docker compose up
 
